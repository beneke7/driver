[project]
name = "landscape-driver"
version = "0.1.0"
description = "Small research harness for adaptive training interventions"
requires-python = ">=3.12"
dependencies = ["torch>=2.14,<2.15"]

[tool.uv]
package = false
